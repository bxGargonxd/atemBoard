#pragma once

#include "Configuration.h"

/**
	* Runs a test programm to check the arduino configurationn
	* \param config configuration for the running programm
*/
void ArduinoTest(Configuration& config);

